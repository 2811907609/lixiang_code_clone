# CodeDoggy AI 代码审查系统

一个基于多智能体架构的自动化代码审查系统，支持 Gerrit 和 GitLab 平台，提供智能的代码质量检查和建议。

## 🏗️ 系统架构

### 分层架构设计

```
┌─────────────────────────────────────────────────┐
│                 服务器层                           │
│  ┌─────────────────┐  ┌─────────────────┐        │
│  │   Gerrit API    │  │   GitLab API    │        │
│  └─────────────────┘  └─────────────────┘        │
└─────────────────────────────────────────────────┘
                        │
┌─────────────────────────────────────────────────┐
│                 工作流层                           │
│  ┌─────────────────────────────────────────────┐  │
│  │         ReviewWorkflow                      │  │
│  │   - 事件处理                                  │  │
│  │   - 上下文管理                                │  │
│  │   - 任务分发                                  │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
                        │
┌─────────────────────────────────────────────────┐
│                 监督层                             │
│  ┌─────────────────────────────────────────────┐  │
│  │     CodeReviewSupervisorAgent               │  │
│  │   - 任务协调                                  │  │
│  │   - 重试管理                                  │  │
│  │   - 质量控制                                  │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
                        │
┌─────────────────────────────────────────────────┐
│                 执行层                             │
│  ┌─────────────────┐  ┌─────────────────┐        │
│  │   Review Agent  │  │  Verify Agent   │        │
│  │   - 代码审查     │  │  - 质量验证      │        │
│  │   - 问题发现     │  │  - 评分机制      │        │
│  └─────────────────┘  └─────────────────┘        │
└─────────────────────────────────────────────────┘
```

## 📁 项目结构

```
ai_agents/
├── sop_workflows/code_review/           # 核心工作流配置
│   ├── micro_agents/                    # 子智能体配置
│   │   ├── review.yaml                  # 审查智能体配置
│   │   └── verify.yaml                  # 验证智能体配置
│   ├── sop.md                          # sop文档
│   └── README.md                       # 本文档
│
├── supervisor_agents/codereview/        # 监督智能体实现
│   ├── agent.py                        # 主监督智能体
│   ├── codereview_yaml_agent.py        # YAML配置智能体
│   └── call_tracker.py                 # 调用追踪器
│
├── modules/codedoggy/                   # 核心业务模块
│   ├── agent/                          # 智能体实现
│   │   ├── review_agent.py             # 审查智能体逻辑
│   │   ├── comment.py                  # 评论处理
│   │   └── score.py                    # 评分系统
│   ├── server/                         # 服务器处理
│   │   ├── handler.py                  # 请求处理器
│   │   └── workflow.py                 # 工作流定义
│   └── tools/                          # 工具集
│
├── tools/codereview/                   # 专用工具
│   ├── file_ops/                       # 文件操作
│   │   └── review_content_manager.py   # 审查内容管理
│   └── __init__.py                     # 工具导出
│
└── micro_agents/                       # 微智能体框架
    ├── yaml_agent_factory.py           # YAML智能体工厂
    └── base_micro_agent.py             # 微智能体基类
```

## 🔄 完整工作流程

### 1. 服务启动与事件解析
**入口**: [`start_review_server()`](../../../modules/codedoggy/server/api.py:178)
- 从环境变量获取服务器类型 (gerrit/gitlab)
- 解析Webhook事件载荷，提取关键信息
- 创建对应平台的API客户端 (GerritClient/GitLabClient)
- 构建统一的Event对象包含仓库信息、提交SHA、项目ID等

**Gerrit事件处理**:
- URL解析: `/c/` → `/a/` 格式转换
- 提取: changeNum, revisionId, sourceCommit, targetCommit
- 创建GerritClient用于后续API调用

**GitLab事件处理**:
- 区分服务器: gitlab vs gitlabee
- 提取: projectID, mergeRequestId, baseSha, headSha, startSha
- 使用访问令牌创建GitLabClient

### 2. 主处理器调度
**调度中心**: [`handler(event)`](../../../modules/codedoggy/server/handler.py)
- 遥测数据收集和性能监控
- 获取仓库代码和diff上下文
- 语言检测和配置管理
- 文件级任务分解和并行处理

### 3. 文件级审查任务
**任务分发**: 为每个变更文件创建独立的FileReviewEvent
- 生成唯一任务ID: `task_codedoggy:{server}:{event_id}:review_{hash}`
- 创建审查内容文件: `~/.cache/codeDoggy/review_content/{task_id}_content.json`
- 根据不同审查模式准备对应的内容结构
- 支持并行处理多个文件

### 4. 监督智能体协调
**协调中心**: [`CodeReviewSupervisorAgent`](../../../supervisor_agents/codereview/agent.py)
- 初始化监督智能体并传递任务ID
- 通过SOP工作流指导任务拆解和智能体调用
- 管理重试机制和质量控制
- 防止模拟执行，确保真实Agent调用

### 5. 子智能体执行
```
监督智能体调用流程:
├── Review Agent (review.yaml)
│   ├── Hook注入审查内容到参数
│   ├── 根据review_type执行不同审查逻辑
│   ├── 生成结构化审查结果
│   └── 返回JSON格式响应
│
└── Verify Agent (verify.yaml)
    ├── 验证审查结果质量
    ├── 计算综合评分 (0-100)
    ├── 决策: ≥50通过, <50触发重试
    └── 返回验证决策
```

### 6. 结果聚合与输出
- 收集所有文件的审查结果
- 过滤和评分处理
- 格式化为平台特定的评论格式
- 通过API客户端发布到Gerrit/GitLab
- 自动清理临时文件和缓存

### 7. 完整数据流
```
Webhook → start_review_server() → Event解析 → handler() →
文件分解 → review_agent() → 监督智能体 →
Hook注入 → 子智能体执行 → 结果验证 →
重试逻辑 → 结果聚合 → API发布 → 资源清理
```

## 🔗 多智能体上下文传递机制

### 核心设计
本系统采用**文件中介 + 任务ID追踪**的上下文传递机制，通过统一的数据格式和调用追踪确保多智能体间的协同工作。

### 任务ID与追踪
- **任务ID格式**: `task_codedoggy:{server}:{event_id}:review_{hash}`
- **调用验证**: Hook机制防止智能体模拟执行，确保真实调用

### 审查内容文件结构
审查内容文件根据不同的`review_type`包含相应的字段组合：

#### diff模式 - Git差异审查
```json
{
  "review_type": "diff",
  "content": {
    "diff_content": "diff --git a/src/main.py...",
    "commit_metadata": {"source_commit": "abc123", "target_commit": "def456"},
    "files_changed": ["src/main.py", "src/utils.py"]
  }
}
```

#### file模式 - 单文件审查
```json
{
  "review_type": "file",
  "content": {
    "target_file": "src/app/main.py",
    "full_file_contents": {
      "src/app/main.py": "完整文件内容..."
    }
  }
}
```


#### snippet模式 - 代码片段审查
```json
{
  "review_type": "snippet",
  "content": {
    "snippet_code": "def hello_world():\n    print('Hello')",
    "context_info": "函数定义片段"
  }
}
```

### 智能体间数据流转
1. **监督智能体**创建包含具体审查内容的上下文文件
2. **Hook机制**在工具调用前自动读取审查内容文件并注入到参数中
3. **子智能体**直接接收完整的审查内容，无需主动获取文件
4. **调用追踪器**记录真实调用状态，防止模拟执行
5. **结果标准化**通过post-hook将返回结果统一转换为JSON字符串格式

### 上下文传递优势
- **数据一致性**: 统一的JSON格式和字段规范
- **可追溯性**: 任务ID贯穿整个执行过程
- **安全性**: 路径验证和调用防护机制
- **可靠性**: 重试时保持上下文，异常时自动清理
- **并发支持**: 基于任务ID的文件隔离机制

## ️ 核心组件

### 监督智能体 (CodeReviewSupervisorAgent)

**职责：**
- 任务协调与分发
- 子智能体管理
- 质量控制与重试
- 结果聚合与验证

**关键特性：**
- 基于SOP工作流驱动
- 支持智能重试机制
- 内置质量评分系统
- 自动文件路径处理

### 审查智能体 (Review Agent)

**配置文件：** [`micro_agents/review.yaml`](micro_agents/review.yaml)

**核心功能：**
- 代码理解与分析
- 问题识别与分类
- 安全漏洞检测
- 性能问题发现
- 最佳实践检查

**输出格式：**
```json
{
  "task_id": "task_codedoggy:gerrit:123:review_abc123",
  "status": "SUCCESS",
  "summary": "审查总结",
  "issues": [
    {
      "type": "潜在问题",
      "severity": "中等",
      "description": "具体问题描述",
      "suggestion": "修改建议",
      "relevantFile": "src/main.py",
      "lineNumber": 42
    }
  ]
}
```

### 验证智能体 (Verify Agent)

**配置文件：** [`micro_agents/verify.yaml`](micro_agents/verify.yaml)

**验证标准：**
- 问题描述准确性 (30%)
- 建议实用性 (25%)
- 技术深度 (20%)
- 安全性评估 (15%)
- 文档完整性 (10%)

**评分机制：**
- ≥50分：审查通过
- <50分：触发重试


## 📊 任务追踪

### 任务ID格式
```
task_codedoggy:{server}:{event_id}:review_{hash}
```

### 文件管理
```
~/.cache/codeDoggy/review_content/
├── task_codedoggy_gerrit_123_review_abc123_content.json
├── task_codedoggy_gitlab_456_review_def456_content.json
└── ...
```

## 📖 文档索引

- [SOP文档](sop.md) - 完整的标准操作程序
- [审查智能体配置](micro_agents/review.yaml) - Review Agent详细配置
- [验证智能体配置](micro_agents/verify.yaml) - Verify Agent详细配置
