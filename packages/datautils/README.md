
## datautils
utils to load/store/restore data from databases, and tools deal with dataframe.


### parquet
You may need parquet related package if you want to cache df in local parquet.
You can install pyarrow like `uv add pyarrow`.
